// Multi-schema source-file disambiguator slug between basename and
// artifact suffix — see Master plan Decision #6. The reader matches
// the suffix only, so the slugged form is picked up automatically.
export type Tenant = { id: string };
