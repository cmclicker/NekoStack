# Basic walk-workspace fixture

Three files in this directory:

- `alpha.schema.ts` — matches the default pattern.
- `beta.schema.js` — matches the default pattern (`.js` branch of
  the brace expansion).
- `not-a-schema.ts` — does NOT match (no `.schema.` suffix).
- `readme.md` — does NOT match.
