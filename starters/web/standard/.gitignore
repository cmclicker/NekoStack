# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node
node_modules/
dist/
dist-ssr/
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# IMPORTANT: NekoStack Generated Artifacts are NOT gitignored.
# The `neko:check` CI pipeline requires generated artifacts (zod.ts, types.ts, etc.)
# to be committed to the repository so it can detect "Silent Lies" (drift).
